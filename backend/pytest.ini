[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    unit: fast isolated unit tests
    integration: api and db integration tests
    security: access control and session security tests
    slow: long-running tests
addopts = -q